# See https://pre-commit.com
# Runs the project's CI script (format, lint, tests, build) before allowing a commit.

repos:
  - repo: local
    hooks:
      - id: ci
        name: ci
        entry: hatch run ci
        language: system
        pass_filenames: false
        stages: [pre-commit]
